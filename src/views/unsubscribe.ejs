
<div class="max-w-md mx-auto text-center py-16">
  <% if (success) { %>
    <h1 class="text-2xl font-bold mb-2">Unsubscribed</h1>
    <p class="text-stone-400">You've been removed from all Portal.Place Intel emails.</p>
  <% } else { %>
    <h1 class="text-2xl font-bold mb-2">Already Unsubscribed</h1>
    <p class="text-stone-400">This link has already been used or is no longer valid.</p>
  <% } %>
</div>
