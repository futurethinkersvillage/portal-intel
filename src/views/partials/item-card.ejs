<div class="bg-stone-900/50 border border-stone-800/50 rounded-xl p-5 hover:border-stone-700/60 transition-colors group">
  <div class="flex items-start gap-4">
    <div class="flex-1 min-w-0">
      <!-- Meta row -->
      <div class="flex flex-wrap items-center gap-2 mb-2.5">
        <span class="text-xs font-medium px-2.5 py-0.5 rounded-md shrink-0"
              :class="categoryBadgeClass(item.category)"
              x-text="categoryLabel(item.category)"></span>
        <span class="text-[11px] text-stone-600 uppercase tracking-wider font-medium" x-text="item.region"></span>
        <template x-if="item.expires_at">
          <span class="text-xs text-amber-400/90 font-medium flex items-center gap-1">
            <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
            <span x-text="formatDate(item.expires_at)"></span>
          </span>
        </template>
        <template x-if="item.pinned_at">
          <span class="text-xs text-amber-400 font-medium flex items-center gap-1">
            <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20"><path d="M5 5a2 2 0 012-2h6a2 2 0 012 2v14l-5-2.5L5 21V5z"/></svg>
            Pinned
          </span>
        </template>
        <template x-if="item.save_count > 0">
          <span class="text-[11px] text-stone-600 ml-auto flex items-center gap-1">
            <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
            <span x-text="item.save_count"></span>
          </span>
        </template>
      </div>

      <!-- Title -->
      <a :href="item.url" target="_blank" rel="noopener noreferrer"
        class="font-semibold text-[15px] text-stone-100 hover:text-brand-300 transition-colors leading-snug block mb-2"
        x-text="item.title"></a>

      <!-- AI Summary -->
      <template x-if="item.summary">
        <p class="text-sm text-stone-400 leading-relaxed mb-3 line-clamp-2" x-text="item.summary"></p>
      </template>

      <!-- AI Actionability chip -->
      <template x-if="item.ai_actionability">
        <div class="inline-flex items-center gap-1.5 text-xs text-brand-300 bg-brand-900/40 border border-brand-800/40 px-2.5 py-1 rounded-lg font-medium mb-3">
          <svg class="w-3 h-3 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/></svg>
          <span x-text="item.ai_actionability"></span>
        </div>
      </template>

      <!-- Footer -->
      <div class="flex items-center gap-2 text-[11px] text-stone-600">
        <span x-text="item.source_name || 'Community'"></span>
        <span class="w-1 h-1 rounded-full bg-stone-700"></span>
        <span x-text="formatDate(item.collected_at)"></span>
      </div>
    </div>

    <!-- Actions -->
    <div class="flex flex-col items-end gap-2 shrink-0 pt-0.5">
      <a :href="item.url" target="_blank" rel="noopener noreferrer"
        class="text-xs bg-stone-800/80 hover:bg-stone-700/80 text-stone-300 hover:text-white px-3.5 py-2 rounded-lg transition-colors font-medium whitespace-nowrap inline-flex items-center gap-1.5 active:scale-[0.97]">
        View
        <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/></svg>
      </a>
      <button @click="toggleSave(item.id)"
        class="p-2 rounded-lg hover:bg-stone-800/60 transition-colors"
        :class="savedIds.has(item.id) ? 'text-brand-400' : 'text-stone-700 hover:text-stone-400'"
        :aria-label="savedIds.has(item.id) ? 'Remove from watchlist' : 'Save to watchlist'">
        <svg class="w-4 h-4" :fill="savedIds.has(item.id) ? 'currentColor' : 'none'" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"/>
        </svg>
      </button>
    </div>
  </div>
</div>
